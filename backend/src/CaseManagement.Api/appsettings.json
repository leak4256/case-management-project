{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "_comment": "Intentionally empty. The connection string carries a credential and must never be committed. Supply it with `dotnet user-secrets` locally, or the ConnectionStrings__CaseManagementDb environment variable in Docker. See README.md.",
    "CaseManagementDb": ""
  },
  "Cors": {
    "AllowedOrigins": [ "http://localhost:4200" ]
  }
}
